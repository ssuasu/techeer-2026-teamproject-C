apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: carpool-ingress
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: carpool-app-service
                port:
                  number: 80